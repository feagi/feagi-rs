# feagi-rs consumes the in-progress feagi-core rewrite directly as path dependencies
# (see `[dependencies]` in Cargo.toml), so no `[patch.crates-io]` overrides are needed.
#
# Paths assume `feagi-rs` and `feagi-core` are side-by-side, e.g. FEAGI-2.0/feagi-rs and
# FEAGI-2.0/feagi-core.
